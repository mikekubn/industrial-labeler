# Deploy to Railway

Production deploys use **GitHub** as the source of truth and **Railway** to build and run the app from this repo’s **Dockerfile**. The [GitHub CLI](https://cli.github.com/) (`gh`) is the usual way to work with the remote repo from your machine (auth, pushes, PRs, and checks) before Railway picks up changes.

